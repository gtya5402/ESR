# qblox_esr

---------

### Description

Python package to operate qblox instrument as an EPR spectrometer

### Installation

qblox_esr can be installed using ``pip``, use ``cd`` to move to the top level directory of the package and run (``-e`` to to be able to edit the package):

```
python -m pip install -e .
```
